# Diagrama de Componentes - API TAIA
### Task Artificial Intelligence Assistant

El siguiente diagrama representa el nivel C4-3 del sistema TAIA. Se realiza zoom sobre el container **API TAIA**, mostrando los principales componentes internos que implementan los módulos Usuario, Academic, AI y Reminders, junto con sus contratos y adaptadores principales.

```mermaid
C4Component
title Diagrama de Componentes (C3) - Contenedor "API TAIA"

%% =========================================================
%% COMPONENTES INTERNOS DEL CONTENEDOR "API TAIA"
%% Para agregar un nuevo módulo funcional: sumar una línea
%% Component(id, "Nombre", "Módulo funcional", "Responsabilidad")
%% dentro de este mismo Container_Boundary.
%% =========================================================
Container_Boundary(api_taia, "API TAIA") {
    Component(usuario, "Usuario", "Módulo funcional", "Registro, autenticación y vinculación de cuenta con Telegram")
    Component(academic, "Academic", "Módulo funcional", "Registro, consulta y actualización de tareas académicas")
    Component(ai, "AI", "Módulo funcional", "Asistente conversacional que interpreta mensajes y genera respuestas")
    Component(reminders, "Reminders", "Módulo funcional", "Creación, edición y notificación de recordatorios")
}

%% =========================================================
%% SISTEMAS EXTERNOS
%% Para agregar uno nuevo: System_Ext(id, "Nombre", "Descripción")
%% =========================================================
System_Ext(telegram, "Telegram", "Plataforma de mensajería utilizada para el envío de notificaciones")
System_Ext(llm, "Proveedor LLM", "Servicio externo de modelos de lenguaje (ej. Gemini)")

%% =========================================================
%% RELACIONES
%% Formato: Rel(origen, destino, "acción", "protocolo/mecanismo")
%% Agregar cada relación nueva justificada aquí, agrupada por
%% componente de origen para mantener el orden de lectura.
%% =========================================================

%% Telegram (entrada)
Rel(telegram, ai, "Reenvía el mensaje del estudiante para su interpretación", "Webhook/HTTPS")

%% AI
Rel(ai, academic, "Consulta / solicita operaciones sobre tareas académicas", "Llamada interna (port/adapter)")
Rel(ai, llm, "Envía prompts y recibe respuestas generadas", "HTTPS/JSON")

%% Reminders
Rel(reminders, academic, "Consulta tareas académicas asociadas a un recordatorio", "Llamada interna (port/adapter)")

%% NOTA: las notificaciones de Reminders se generan y entregan
%% dentro de la propia app (no salen a un sistema externo).
%% No se dibuja relación saliente hasta confirmar el mecanismo
%% real de entrega (push, polling desde app móvil, etc.).

%% Ajustes de layout para evitar solapamiento de etiquetas
%% sobre el componente "academic". Los valores son un punto
%% de partida; ajustar offsetX/offsetY según cómo renderice
%% tu visor de Mermaid.
UpdateRelStyle(ai, academic, $offsetX="-40", $offsetY="-20")
UpdateRelStyle(reminders, academic, $offsetX="-40", $offsetY="20")
UpdateRelStyle(ai, llm, $offsetX="20", $offsetY="-30")
UpdateRelStyle(telegram, ai, $offsetX="-20", $offsetY="-40")

%% NOTA DE ESCALABILIDAD:
%% $c4ShapeInRow controla cuántos "cuadros" se muestran por fila.
%% Ajustar este valor cuando el número total de componentes +
%% sistemas externos cambie significativamente, para mantener
%% el diagrama legible.
UpdateLayoutConfig($c4ShapeInRow="2", $c4BoundaryInRow="1")
