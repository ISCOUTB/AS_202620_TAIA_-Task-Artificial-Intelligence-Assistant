# Diagrama de Contexto - TAIA
### Task Artificial Intelligence Assistant

```mermaid
---
config:
  wrap: true
  c4:
    width: 240
---
C4Context
    title Diagrama de Contexto - TAIA

    Person(estudiante, "Estudiante", "Registra tareas académicas, consulta su información y recibe recordatorios.")
    System_Ext(telegram, "Telegram", "Canal conversacional para capturar tareas y entregar notificaciones.")
    System(taia, "TAIA", "Interpreta lenguaje natural, registra tareas, notifica vencimientos y propone planes de estudio.")
    System_Ext(gemini, "Proveedor LLM (Gemini)", "Interpreta el mensaje y extrae los campos estructurados de la tarea.")

    BiRel(estudiante, telegram, "Escribe y recibe avisos")
    Rel(estudiante, taia, "Consulta tareas (Flutter)")
    BiRel(telegram, taia, "Reenvía mensajes")
    BiRel(taia, gemini, "Interpreta el mensaje")

    UpdateElementStyle(estudiante, $bgColor="#08427b", $fontColor="#ffffff", $borderColor="#052e56")
    UpdateElementStyle(taia, $bgColor="#1168bd", $fontColor="#ffffff", $borderColor="#0b4884")
    UpdateElementStyle(telegram, $bgColor="#999999", $fontColor="#ffffff", $borderColor="#6b6b6b")
    UpdateElementStyle(gemini, $bgColor="#999999", $fontColor="#ffffff", $borderColor="#6b6b6b")

    UpdateRelStyle(estudiante, telegram, $offsetX="-40", $offsetY="-50")
    UpdateRelStyle(estudiante, taia, $offsetX="-160", $offsetY="0")
    UpdateRelStyle(telegram, taia, $offsetX="30", $offsetY="0")
    UpdateRelStyle(taia, gemini, $offsetX="-30", $offsetY="95")

    UpdateLayoutConfig($c4ShapeInRow="2", $c4BoundaryInRow="1")
```

**Convenciones**

| Color | Significado |
| ----- | ----------- |
| Azul oscuro | Persona (actor humano) |
| Azul | Sistema en alcance del proyecto |
| Gris | Sistema externo (no lo construimos ni lo controlamos) |
